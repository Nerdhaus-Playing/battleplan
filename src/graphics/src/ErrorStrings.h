#pragma once

#include <string>

const std::string NOT_CALLABLE_WITHOUT_ARGS = "This command cannot be called without arguments";
const std::string NOT_CALLABLE_WITH_ARGS = "This command cannot be called with arguments";